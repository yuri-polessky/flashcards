<%= simple_form_for card do |f| %>
  <%= f.input :original_text %>
  <%= f.input :translated_text %>
  <%= f.input :deck_id, collection: current_user.decks %>
  <%= t(:or_create_one) %>
  <%= text_field_tag :new_deck_name %>
  <%= f.input :picture, as: :file %>
  <%= f.button :submit %>
<% end %>